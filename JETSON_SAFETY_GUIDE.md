# Jetson Orin Nano 시스템 안전 가이드 및 복구 방법

패키지 설치 중 부팅이 안 되거나 GUI가 뜨지 않는 상황은 매우 드물지만, 주로 **저장 공간 부족**이나 **시스템 중요 파일 손상**으로 인해 발생할 수 있습니다. 예방 방법과 대처법을 정리합니다.

## 1. 부팅/로그인 실패의 주요 원인

1.  **디스크 용량 부족 (가장 흔함 ⚠️)**
    *   OpenCV, PyTorch 등 대형 라이브러리 설치 시 용량이 가득 차면 GUI(데스크탑 환경)가 실행되지 않거나 로그인 화면에서 무한 루프가 발생합니다.
    *   커널 부팅은 되지만 화면이 안 뜨는 경우가 많습니다.
2.  **시스템 Python 손상**
    *   `/usr/bin/python3` 등 시스템 기본 Python을 삭제하거나 심볼릭 링크를 잘못 변경하면 `gnome-terminal` 등 시스템 앱이 마비되어 부팅이 멈춘 것처럼 보일 수 있습니다.
3.  **패키지 충돌**
    *   `apt upgrade`를 함부로 실행하여 JetPack 전용 드라이버(L4T)가 덮어씌워지는 경우 그래픽 드라이버가 깨질 수 있습니다.

## 2. 예방 수칙 ✅

### 설치 전 용량 확인
설치 전에 항상 여유 공간을 확인하세요. 최소 **3~5GB** 이상의 여유 공간을 유지하는 것이 좋습니다.

```bash
df -h
# / (루트) 파티션의 Avail(사용 가능) 공간 확인
```

### 전용 가상환경 사용
시스템 Python을 건드리지 않도록 항상 `venv`를 사용하세요.

```bash
python3 -m venv ~/edgeface_env
source ~/edgeface_env/bin/activate
```

### 시스템 패키지 업그레이드 주의
Jetson에서는 무조건적인 `sudo apt upgrade` 보다는 필요한 패키지만 `install` 하는 것이 안전합니다.

## 3. 사고 발생 시 대처 방법 🚑

### 상황 A: 부팅 로고는 뜨는데 검은 화면이거나 로그인이 안 됨 (GUI 실패)
이 경우 운영체제 자체는 살아있을 확률이 높습니다.

1.  **콘솔모드 진입 (TTY)**
    *   키보드에서 `Ctrl` + `Alt` + `F2` (또는 F3, F4)를 동시에 누릅니다.
    *   검은 화면에 텍스트 로그인 창이 뜨면 아이디/비번을 입력해 로그인합니다.
2.  **용량 확보**
    *   `df -h`로 용량이 100%인지 확인합니다.
    *   불필요한 파일 삭제:
        ```bash
        sudo apt clean
        rm -rf ~/.cache/pip
        # 다운로드 폴더 등 정리
        ```
    *   다시 `sudo reboot` 해봅니다.
3.  **SSH 접속**
    *   화면이 안 나오더라도 네트워크가 연결되어 있다면 다른 PC에서 SSH로 접속 가능할 수 있습니다.
    *   `ssh username@jetson-ip`

### 상황 B: 아예 전원이 안 들어오거나 NVIDIA 로고에서 멈춤
1.  **Serial Console (디버깅용)**
    *   전문적인 방법으로, 보드의 핀에 시리얼 케이블을 연결하여 부팅 로그를 확인해야 원인을 알 수 있습니다.
2.  **Recovery Mode (재설치)**
    *   SD카드를 사용하는 경우: PC 리더기에 꽂아서 데이터를 백업하고 이미지를 다시 굽는 것이 가장 빠릅니다.
    *   NVMe/SSD를 사용하는 경우: SDK Manager를 통해 **Force Recovery Mode**로 진입하여 OS를 다시 플래싱해야 할 수 있습니다.

## 4. 백업 권장 (이미지 통째로 백업)
중요한 세팅이 끝났다면 현재 상태를 백업하는 것이 좋습니다.
*   **SD카드**: 윈도우의 'Win32 Disk Imager' 등을 이용해 "Read" 기능을 써서 현재 SD카드를 .img 파일로 PC에 저장해두세요.
*   **Jetson-Backup**: 중요한 소스 코드는 항상 git push해두는 것이 가장 안전합니다.
